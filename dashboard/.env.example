# SurakshaAR Dashboard environment configuration
# Copy this file to .env and adjust values as needed.

# Base URL of the FastAPI backend
VITE_API_BASE=http://localhost:8000/api/v1

# Set to "true" to force using mock data instead of the real backend
VITE_USE_MOCK=false
