"""Model definitions and exported weights (ONNX / TFLite / Vosk model files).

Model files are too large for version control; place checkpoints here and set
``ml.vision.config.MODEL_PATH`` (or pass ``model_path`` to the detector).
No model weights are shipped with the MVP scaffold — see ml/vision/README.md.
"""